{
  "name": "@repo/api",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/infrastructure/trpc/router.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@repo/shared": "workspace:*",
    "@ai-sdk/anthropic": "^1.2.12",
    "@trpc/server": "^11.1.2",
    "ai": "^4.3.16",
    "better-auth": "^1.2.7",
    "hono": "^4.7.7",
    "mongodb": "^6.15.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
